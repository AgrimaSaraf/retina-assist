'use client'; import {useState} from 'react'; import {analyzeRetina} from '@/lib/api'; export default function P(){const[f,sf]=useState<File|null>(null);const[m,sm]=useState('');async function run(e:any){e.preventDefault();if(!f)return sm('Choose an image first.');try{const r=await analyzeRetina(f);sm(`${r.label} · ${Math.round(r.confidence*100)}%`)}catch(x:any){sm(x.message)}}return <section><div className="eyebrow">ENGINEERING SYSTEM</div><h1>Retinal screening</h1><div className="warning">Research interface only. No clinical-use claim.</div><form className="panel" onSubmit={run}><input type="file" accept="image/jpeg,image/png" onChange={e=>sf(e.target.files?.[0]||null)}/><button>Analyze</button></form><p>{m}</p></section>}
