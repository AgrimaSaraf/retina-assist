# Models

`models/retinaassist_dr.pth` — optional retinal checkpoint.

`models/followup_logistic.joblib` — optional follow-up model. If trained with this repo's generator, it has no clinical validity.
